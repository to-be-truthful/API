import {IController} from "./IController";

export class QuestionController implements IController{
    initRoutes(expressRouter: e.Router) {
    }

    private getQuestion = async (req, res, next) => {

    };
}